// Legacy test file (intentionally left empty).
//
// We removed the nested `sku_selector` + any normalization helpers as part of the
// "flat JSON only" interface change. See the replacement tests:
// - tests/fba-list-replenish-asap-input.test.ts
