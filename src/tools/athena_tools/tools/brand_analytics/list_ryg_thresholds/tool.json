{
  "name": "brand_analytics_list_ryg_thresholds",
  "description": "Lists the RYG (Red/Yellow/Green) signal thresholds used by Brand Analytics analysis tools. Shows both system defaults and any company-specific overrides.\n\nUse cases:\n- Inspect current threshold values before customising them\n- Verify company-specific overrides are applied correctly\n- Understand how strength, weakness, opportunity, and trend signals are classified\n\nEach row defines one threshold boundary for a (tool, signal_group, metric, color) combination.\nCompany overrides (company_ids entry) take priority over defaults (company_id IS NULL in the DB) in analysis queries.",
  "isConsequential": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "company_ids": {
        "type": "array",
        "items": {
          "type": "integer",
          "minimum": 1
        },
        "minItems": 1,
        "description": "Company identifier(s) - required for multi-tenant isolation."
      },
      "tool": {
        "type": "string",
        "enum": [
          "sqp",
          "scp",
          "global",
          "growth_machine"
        ],
        "description": "Optional: filter by tool name (sqp = Search Query Performance, scp = Search Catalog Performance, global = shared trend thresholds, growth_machine = prescription-rule thresholds for the Growth Machine diagnostic tool)."
      },
      "include_defaults": {
        "type": "boolean",
        "default": true,
        "description": "If true (default), include system default thresholds alongside company overrides. Set false to see only company-specific overrides."
      }
    },
    "required": [
      "company_ids"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "items": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "company_id": {
              "type": [
                "integer",
                "null"
              ]
            },
            "is_override": {
              "type": "boolean"
            },
            "tool": {
              "type": "string"
            },
            "signal_group": {
              "type": "string"
            },
            "metric": {
              "type": "string"
            },
            "color": {
              "type": "string"
            },
            "threshold_value": {
              "type": [
                "number",
                "null"
              ]
            },
            "signal_code": {
              "type": "string"
            },
            "signal_description": {
              "type": "string"
            },
            "updated_at": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}
