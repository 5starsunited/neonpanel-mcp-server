{
  "name": "brand_analytics_write_tracked_search_terms",
  "description": "Writes tracked search term entries for a company. Consequential.\n\nUse cases:\n- Seed the company's keyword cores per ASIN / parent / product family\n- Tag keywords with intent (defend | attack | evaluate | branded) and priority\n- Soft-delete entries via action='deactivate'\n- Reset the full tracking set via action='reset'\n\nUpsert key: (company_id, marketplace, LOWER(keyword), asin, parent_asin, product_family).\n\nExample (seed a per-ASIN core and a family-wide keyword):\n{\n  \"company_id\": 103,\n  \"reason\": \"Day-1 onboarding: seed keyword cores.\",\n  \"writes\": [\n    { \"marketplace\": \"US\", \"asin\": \"B0AAAA1111\", \"keyword\": \"moisturizing face cream\", \"priority\": 1, \"intent\": \"defend\" },\n    { \"marketplace\": \"US\", \"product_family\": \"Widgets\", \"keyword\": \"blue widget\",             \"priority\": 2, \"intent\": \"attack\" }\n  ]\n}",
  "isConsequential": true,
  "inputSchema": {
    "type": "object",
    "properties": {
      "company_id": { "type": "integer", "minimum": 1 },
      "reason": { "type": "string", "minLength": 5 },
      "action": {
        "type": "string",
        "enum": ["write", "deactivate", "reset"],
        "default": "write",
        "description": "write (default): upsert entries. deactivate: set is_active=false for the given slots. reset: deactivate ALL tracked-term entries for this company."
      },
      "dry_run": { "type": "boolean", "default": true },
      "writes": {
        "type": "array",
        "minItems": 1,
        "maxItems": 200,
        "items": {
          "type": "object",
          "properties": {
            "marketplace": { "type": "string", "minLength": 1, "maxLength": 10 },
            "keyword": { "type": "string", "minLength": 1, "maxLength": 200 },
            "asin": { "type": ["string", "null"], "maxLength": 20 },
            "parent_asin": { "type": ["string", "null"], "maxLength": 20 },
            "product_family": { "type": ["string", "null"], "maxLength": 200 },
            "priority": { "type": ["integer", "null"], "minimum": 1, "maximum": 5 },
            "intent": {
              "type": ["string", "null"],
              "enum": ["defend", "attack", "evaluate", "branded", null]
            },
            "notes": { "type": ["string", "null"], "maxLength": 500 }
          },
          "required": ["marketplace", "keyword"],
          "additionalProperties": false
        }
      }
    },
    "required": ["company_id", "reason"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "dry_run": { "type": "boolean" },
      "action": { "type": "string" },
      "accepted": { "type": "integer" },
      "written": { "type": "integer" },
      "deactivated": { "type": "integer" },
      "message": { "type": "string" },
      "error": { "type": "string" }
    },
    "required": ["dry_run", "action", "accepted", "written"]
  }
}
