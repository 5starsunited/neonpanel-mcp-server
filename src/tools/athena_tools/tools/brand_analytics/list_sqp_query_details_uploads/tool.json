{
  "name": "brand_analytics_list_sqp_query_details_uploads",
  "description": "Lists persisted Seller Central Search Query Details uploads (screenshot extractions). Read-only.\n\nOne row per (company_id, marketplace, LOWER(keyword), period_start). Re-uploads overwrite the previous row (upsert). Use this tool to verify which keyword/period combinations already have screenshot data before asking the user for another upload.\n\nFilters: company_ids (required), optional marketplaces, keywords (case-insensitive), uploaded_by, period range (any upload whose [period_start, period_end] overlaps the supplied window).",
  "isConsequential": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "company_ids": {
        "type": "array",
        "items": { "type": "integer", "minimum": 1 },
        "minItems": 1
      },
      "marketplaces": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 10 } },
      "keywords": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 200 } },
      "uploaded_by": { "type": "array", "items": { "type": "string", "minLength": 1, "maxLength": 200 } },
      "period_overlap_start": { "type": "string", "format": "date" },
      "period_overlap_end": { "type": "string", "format": "date" },
      "limit": { "type": "integer", "minimum": 1, "maximum": 1000, "default": 200 }
    },
    "required": ["company_ids"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "items": { "type": "array", "items": { "type": "object", "additionalProperties": true } }
    },
    "required": ["items"]
  }
}
