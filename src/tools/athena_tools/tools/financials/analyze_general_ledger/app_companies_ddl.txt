CREATE TABLE neonpanel_iceberg.app_companies (
  id bigint,
  uuid string,
  name string,
  shortname string,
  customer_id string,
  organization_id bigint,
  consolidator int,
  address_line1 string,
  address_line2 string,
  city string,
  country string,
  postal_code string,
  state string,
  phone string,
  currency string,
  timezone string,
  show_onboarding boolean,
  entry_locked_date date,
  cogs_allocation string,
  fifo_mode string,
  fifo_auto boolean,
  fifo_check boolean,
  fifo_state string,
  fifo_version int,
  estimated_cost boolean,
  deleted_at timestamp,
  created_at timestamp,
  updated_at timestamp,
  partition_name string)
LOCATION 's3://etl-glue-amazon-ads-prod-preprocessbucketreports6-1w0usrm0kq0j7/aws_etl/neonpanel_iceberg/neonpanel_iceberg/app_companies'
TBLPROPERTIES (
  'table_type'='iceberg',
  'compression_level'='3',
  'format'='PARQUET',
  'write_compression'='ZSTD'
);