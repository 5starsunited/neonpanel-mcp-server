CREATE TABLE brand_analytics_iceberg.marketing_stream_snapshot (
  ad_date date,
  marketplace_id string,
  company_id bigint,
  dataset string,
  time_window_start date,
  campaign_id string,
  adgroup_id string,
  ad_id string,
  target_keyword_id string,
  target keyword string,
  placement string,
  match type string,
  currency string,
  cost double,
  impressions bigint,
  clicks bigint,
  attributed sales double,
  conversions bigint,
  attributed units ordered bigint,
  advertised sku string,
  advertised asin string,
  purchased asin string,
  campaign name string,
  ad group name string,
  ads sku key string)
PARTITIONED BY (`company_id`, `marketplace_id`, `ad_date`)
LOCATION 's3://etl-glue-amazon-ads-prod-preprocessbucketreports6-1w0usrm0kq0j7/aws_etl/brand_analytics_iceberg/brand_analytics_iceberg/marketing_stream_snapshot'
TBLPROPERTIES (
  'table_type'='iceberg',
  'compression_level'='3',
  'format'='PARQUET',
  'write_compression'='ZSTD'
);